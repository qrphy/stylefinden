// Outfit sayfaları veri çekerken gösterilen loading durumu.
// Henüz iskelet UI yoktur; geçici olarak NotFound bileşeni render edilir.
import NotFound from "@/app/not-found"

export default function Loading() {
  return <NotFound />
}
